build:
    tests:
        override:
            -
                command: 'vendor/bin/phpunit ./tests --whitelist=./src --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'clover'
