build:
    tests:
        override:
            -
                command: 'vendor/bin/phpunit ./tests --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'clover'
